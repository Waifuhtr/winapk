<?xml version="1.0" encoding="utf-8" standalone="no"?>
<!--
  Tek-oyun port manifest'i.

  Upstream'e göre farklar:
    * Giriş noktası MainActivity DEĞİL, GameLauncherActivity.
    * MainActivity / ControlsEditorActivity / ExternalControllerBindingsActivity
      tanımlı KALDI ama LAUNCHER filtreleri yok. Silmedik çünkü upstream kodu
      (ör. Input Controls diyaloğu) onlara referans veriyor; menüden erişim
      GamePortMenu ile kapatıldığı için kullanıcı bunlara ulaşamıyor.
    * android:label bir placeholder'dan geliyor (build.gradle -> port.properties).
    * FileProvider authority'si ${applicationId}'ye bağlandı; sabit bırakılsaydı
      cihazda gerçek Winlator kuruluyken kurulum reddedilirdi.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:appCategory="game"
        android:isGame="true"
        android:extractNativeLibs="true"
        android:allowAudioPlaybackCapture="true"
        android:allowBackup="false"
        android:label="${gamePortLabel}">

        <!-- TEK giriş noktası: kurulum + doğrudan oyun başlatma. -->
        <activity android:name="com.winlator.gameport.GameLauncherActivity"
            android:theme="@style/AppThemeFullscreenDark"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Oyunun asıl çalıştığı ekran (Winlator'ın kendi X sunucusu). -->
        <activity android:name="com.winlator.XServerDisplayActivity"
            android:exported="false"
            android:theme="@style/AppThemeFullscreenDark"
            android:supportsPictureInPicture="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|navigation"
            android:screenOrientation="sensorLandscape" />

        <!-- Aşağıdakiler kullanıcıya kapalı; sadece upstream kod yollarının
             kırılmaması için tanımlı bırakıldı. LAUNCHER filtreleri yok. -->
        <activity android:name="com.winlator.MainActivity"
            android:theme="@style/AppThemeDark"
            android:exported="false"
            android:screenOrientation="sensor"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|navigation" />

        <activity android:name="com.winlator.ControlsEditorActivity"
            android:exported="false"
            android:theme="@style/AppThemeFullscreenDark"
            android:screenOrientation="sensorLandscape" />

        <activity android:name="com.winlator.ExternalControllerBindingsActivity"
            android:theme="@style/AppThemeDark"
            android:exported="false"
            android:screenOrientation="sensor" />

        <!-- Uygulama arka plandayken / ekran kilitliyken container'ı ayakta tutar. -->
        <service
            android:name="com.winlator.services.ForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="false">
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>
</manifest>
